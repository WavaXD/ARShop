cloud:
  aws:
    credentials:
      access-key: 
      secret-key:
    region:
      static: ap-southeast-1
    stack:
      auto: false

application:
  bucket:
    name: arshopimage


spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB

server:
  port: 8080
